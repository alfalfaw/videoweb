import Vue from 'vue'
import Vuetify from 'vuetify'
//云端注释
import 'vuetify/dist/vuetify.min.css'
//云端注释
Vue.use(Vuetify)

const opts = {
  theme: {
    dark: true
  }
}

export default new Vuetify(opts)
